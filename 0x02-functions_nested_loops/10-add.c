#include "main.h"
/**
 * add - function that adds two integers and returns the result.
 * @one: first input
 * @two: second input
 *
 * Return: a + b
 */
int add(int one, int two)
{
	return (one + two);
}
